export class ScoreModel {
    constructor(props) {
        this.playerName = props.playerName;
        this.score = props.score;
        this.stage = props.stage;
        this.scoreDate = props.scoreDate;
    }
}